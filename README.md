# GSD Skills

GSD(Get Ship Done) 참가자를 위한 Claude Code 스킬 모음.

| 스킬 | 하는 일 |
|---|---|
| [gsd-submit](skills/gsd-submit/) | 오늘 만든 프로젝트를 밀그램 이벤트 페이지에 제출물로 등록 — 내용 정리부터 화면 캡처, 최종 제출까지 |

> **처음 쓰시나요? → [수강생 가이드](GUIDE.md)**
> 화면 캡처와 함께 처음부터 끝까지 설명되어 있습니다.

## 설치

터미널에 아래 한 줄을 붙여넣고 실행한다.

```bash
git clone https://github.com/onedegreelabs/gsd-skills ~/gsd-skills && mkdir -p ~/.claude/skills && ln -sfn ~/gsd-skills/skills/gsd-submit ~/.claude/skills/gsd-submit
```

그리고 **Claude Code를 새로 실행한다.** 이미 켜져 있던 창에는 잡히지 않는다.

## 쓰는 법

오늘 작업한 프로젝트 폴더에서 Claude Code를 켜고 이렇게 말하면 된다.

```
밀그램에 제출해줘
```

그러면 알아서 이렇게 한다.

1. 오늘 Claude와 나눈 작업 기록과 코드를 읽어 **무엇을 만들었는지** 정리한다
2. 만든 서비스 화면을 **캡처**해 대표 이미지로 쓴다
3. 오늘 열린 **기수 이벤트**를 찾아 빈 슬롯에 팀을 만들고
4. 제출물을 등록하고 **공개 링크**를 알려준다

내용을 고치고 싶으면 그대로 말하면 된다 — "회고 부분 다시 써줘", "이미지 다른 걸로 바꿔줘".
다시 제출하면 같은 자리를 덮어쓴다.

## 처음 쓸 때

**최초 1회 세팅**이 필요하다. 설치는 전부 자동이고,
직접 할 일은 마지막에 열리는 Chrome 창에서 **밀그램 로그인 한 번**뿐이다.
스킬이 알아서 점검하고 안내하지만, 미리 해두고 싶으면 직접 실행해도 된다.

```bash
~/.claude/skills/gsd-submit/scripts/install.sh
```

로그인은 **GSD 참가 신청에 쓴 계정**으로 해야 한다. 한 번 하면 다음 기수부터는 로그인도 없다.
자세한 내용은 [setup.md](skills/gsd-submit/references/setup.md).

## 자주 묻는 것

**제출 마감이 지났어요** — 운영자에게 말한다. 마감은 운영자만 열 수 있다.

**팀으로 참가했어요** — 팀장 계정에서 한 번만 제출하면 된다. 한 이벤트에서 한 사람은 한 팀에만 속한다.

**웹 서비스가 아니라 자동화 스크립트를 만들었어요** — 실행 결과물(만들어진 시트, 리포트, 슬랙 메시지)을
찍어서 올린다. 스킬이 알아서 물어보거나 찾는다.

**이미 손으로 등록했는데 내용만 고치고 싶어요** — 그냥 "다시 제출해줘" 하면 기존 제출물을 갱신한다.

## 고칠 때

`SKILL.md` 본문은 500줄 이내로 유지하고 세부 내용은 `references/` 로 뺀다.
밀그램 API나 UI에서 새로 걸린 문제를 찾으면
[milgram-api.md](skills/gsd-submit/references/milgram-api.md) 에 **동작하는 코드와 함께** 남긴다.
다음 기수 사람이 같은 벽에 부딪히지 않는 게 이 문서의 목적이다.
